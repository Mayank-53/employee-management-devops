apiVersion: v1
kind: Service

metadata:
  name: employee-backend-service
  namespace: employee-management

spec:
  selector:
    app: employee-backend

  ports:
  - port: 8000
    targetPort: 8000

  type: ClusterIP
