apiVersion: v1
kind: Service

metadata:
  name: employee-postgres-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: postgres

  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: {{ .Values.postgres.service.port }}

  type: ClusterIP
